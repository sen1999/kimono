<%= form_for @tansu, remote: :true, :url => {:action => :create}) do |f| %>
<%= f.text_field :memo, placeholder: "例）  縦縞" %>
<%= f.file_field :image, placeholder: "画像アップロード" %>
<%= f.submit "たんすに入れる" %>
<% end %>

<ul>
  <%= render @tansus %>
</ul>