<%= form_for @item do |f| %>
  <%= f.text_field :memo, placeholder: "例）  縦縞" %>
  <%= f.file_field :image, placeholder: "画像アップロード" %>
  <%= f.submit "たんすに入れる" %>
<% end %>

<br>


<% @items.each do |item| %>

  <%= render partial: "item", locals: { item: item } %>

<% end %>

